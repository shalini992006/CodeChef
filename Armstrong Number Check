n = int(input("Enter a number: "))
temp = n
sum = 0
digits = len(str(n))

while temp > 0:
    r = temp % 10
    sum += r ** digits
    temp //= 10

if sum == n:
    print("Armstrong Number")
else:
    print("Not an Armstrong Number")
